export type CondominoParams = {
  id?: number;  
  apartamento: number | string;
  condominio?: number;
  name: string;
  cpf: string;
  sindico: string;
  tipo: string;
  numeroquartos: number | string;
}
